node_modules/
.next/
out/
build/

# Prisma's Rust-free generated client (generator client { output = ... } in
# prisma/schema.prisma) - regenerated by `prisma generate` (wired into `npm
# run build`), and its output embeds an absolute, machine-specific source
# path, so it must never be committed.
/src/generated/
.env
.env*.local
*.tsbuildinfo
next-env.d.ts
coverage/
playwright-report/
test-results/
graphify-out/
.vercel

# Local database backups
syveka-backup.sql

# Personal Claude Code settings (permissions allowlist) - never shared
.claude/settings.local.json
